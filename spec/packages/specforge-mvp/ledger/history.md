# History Ledger — specforge-mvp

Status: Draft

Append-only timeline of status transitions, content events, and provenance updates.

| Date | LedgerId | Event | Detail |
|---|---|---|---|
| 2026-05-25 | `ART-README-TOP` | Created | Project top-level README established. |
| 2026-05-25 | `ART-PACKAGE-README` | Created | Initial package scaffold established. |
| 2026-05-25 | `ART-WORK-PLAN` | Created | Stage 0 / Stage 1 / Stage 2 / Stage 3 outlined. |
| 2026-05-25 | `ART-WORK-LEDGER` | Created | Dashboard initialized. |
| 2026-05-25 | `ART-LIFECYCLE` | Created | Lifecycle policy and stable reference rule recorded. |
| 2026-05-25 | `ART-GLOSSARY` | Created | Core terms recorded; spec-kit overloaded terms listed as avoided. |
| 2026-05-25 | `ART-IMPACT` | Created | Impact aspect list recorded. |
| 2026-05-25 | `ART-ITEM-CONTRACT` | Created | Required sections for item specs recorded. |
| 2026-05-25 | `ART-TEMPLATE-DECISION` | Created | Decision record template recorded. |
| 2026-05-25 | `ART-TEMPLATE-ITEM` | Created | Item spec template recorded. |
| 2026-05-25 | `ART-DECISIONS-README` | Created | Decision conventions for this package recorded. |
| 2026-05-25 | `ART-LEDGER-README` | Created | Ledger schema recorded. |
| 2026-05-25 | `ART-LEDGER-ARTIFACTS` | Created | Initial artifact rows added. |
| 2026-05-25 | `ART-DEC-001` | Drafted | First decision drafted: Distribution and Transport. |
| 2026-05-25 | `ART-DEC-001` | Scope clarified | Instruction layer moved out to planned DEC-008 after user-AI discussion of MCP instruction patterns (tool descriptions vs resources vs skills vs CLAUDE.md vs AGENTS.md). |
| 2026-05-25 | `ART-WORK-PLAN` | Updated | Stage 0 expanded from 7 to 8 decisions; DEC-008-INSTRUCTION-LAYER-DESIGN added as last Stage 0 record because it depends on DEC-005 and DEC-007. |
| 2026-05-25 | `ART-DEC-008` | Planned | Slot reserved in Stage 0 for instruction-layer design: behavioral guidance split across tool descriptions, MCP Resources, skills, target-project CLAUDE.md, and shipped AGENTS.md. |
| 2026-05-25 | `ART-DEC-001` | Refined detail | Publish path made explicit: `dotnet publish -o D:\Work\specforge\bin\` keeps the registered binary path framework-agnostic so `.mcp.json` does not need updating when DEC-003 picks the framework. Caught during user review of the registration example. |
| 2026-05-25 | `ART-DEC-001` | Multi-agent nuance | User pointed out that "no agent-specific files" was too sweeping because DEC-008 will ship skills, and skill formats are per-agent by nature. Section rewritten to distinguish cross-agent MCP-tool layer from per-agent skill layer. |
| 2026-05-25 | `ART-DEC-001` | Agent scope refined | User clarified: Cursor not used in organization (explicit exclusion); Codex CLI used heavily (must be in MVP, not deferred). Multi-Agent Strategy now lists Claude Code + Codex CLI as supported. Exact Codex skill format (AGENTS.md vs .codex/commands/) deferred to DEC-008. New consequence: each skill packaged twice in MVP. |
| 2026-05-25 | `ART-DEC-001` | Codex mechanism researched | Web-searched OpenAI Codex documentation (developers.openai.com/codex/skills). Findings: Codex has Agent Skills with the same `SKILL.md` directory format as Claude Code; USER scope is `~/.agents/skills/<name>/SKILL.md`; required frontmatter `name` and `description`. Codex's older `~/.codex/prompts/` (Custom Prompts) is deprecated. spec-kit's `.codex/commands/` table is outdated. DEC-001 updated: "two install locations" is the accurate cost, not "two file formats". Codex's optional `agents/openai.yaml` allows declaring MCP tool dependencies — specforge will exploit this. Sources added to DEC-001 Source Links. |
| 2026-05-25 | `ART-DEC-001` | Approved | User approved DEC-001 after research-driven refinements (publish path `-o`, multi-agent scope refined, Codex skills aligned). DEC-001 is now a reference input for DEC-002, DEC-003, DEC-005, DEC-008, and ITEM-003. Logged as `REV-DEC-001-001` in reviews.md. |
| 2026-05-25 | `ART-LEDGER-REVIEWS` | Status promoted | Placeholder → Draft: first review event (`REV-DEC-001-001`) recorded. |
| 2026-05-25 | `ART-DEC-002` | Design forks resolved | User answered structured questions (AskUserQuestion): config-file discovery, named session-scoped package selection, repo-level shared with per-package override. All three were the recommended options. |
| 2026-05-25 | `ART-DEC-002` | Drafted | DEC-002 drafted from the Q&A answers plus drafted mechanics (config schema v1, single/multi package default behavior, file-level override). Three open questions flagged for review. |
| 2026-05-25 | `ART-DEC-002` | Open questions resolved | Second AskUserQuestion round resolved 2 of 3 open questions: override mechanism = convention-only (filesystem is single source of truth; no config field); defaultPackage = omitted from MVP. Selection-error payload formally deferred to DEC-007. DEC-002 ready for final approval. |
| 2026-05-28 | `ART-DEC-002` | Approved | User approved DEC-002. DEC-002 is now a reference input for DEC-003, DEC-004, DEC-006, DEC-007, ITEM-002, and ITEM-010. Logged as `REV-DEC-002-001` in reviews.md. Stage 0 now has 2 of 8 decisions approved; DEC-003-RUNTIME-AND-ARCHITECTURE is next. |
| 2026-05-28 | `ART-DEC-003` | Design forks resolved | User answered structured questions (AskUserQuestion): runtime = .NET 10 LTS; layout = Core + Mcp + Tests; publish mode = self-contained single-file (no trim, no AOT). All three were the recommended options. |
| 2026-05-28 | `ART-DEC-003` | Drafted | DEC-003 drafted from the Q&A answers plus filled-in mechanics: project layout under `src/` and `test/`, dependency direction enforced by architecture test, publish command pinned with `-o` for framework-agnostic path, baseline libraries listed (Markdig, YamlDotNet, ModelContextProtocol, xUnit), stderr-only logging convention, typed Core exceptions with MCP envelope deferred to DEC-007. |
| 2026-05-28 | `ART-DEC-003` | Approved | User approved DEC-003 on first review (no refinements). DEC-003 is now the runtime/architecture foundation for DEC-004 → DEC-008 and for ITEM-001 (solution bootstrap), ITEM-002 (config model). Logged as `REV-DEC-003-001` in reviews.md. Stage 0 now has 3 of 8 decisions approved; DEC-004-ID-SCHEME-CUSTOMIZATION is next. |
| 2026-05-28 | `ART-DEC-004` | Design forks resolved | User answered structured questions (AskUserQuestion): number width = uniform 3-digit; customization = additive only; review IDs = composite `REV-<target>-<seq>`; commit IDs = sequential `CMT-NNN` + git-ref column. All four were the recommended options. |
| 2026-05-28 | `ART-DEC-004` | Drafted | DEC-004 drafted from Q&A answers plus filled-in mechanics: canonical regex set (`^[A-Z]{2,6}-[0-9]{3}$` and composite REV), reserved core kinds `{DEC, ITEM, ART, REV, CMT}` with no HIST kind, two-flavor `ART-` form (mirroring vs descriptive), per-package counter scope, identifier immutability and supersession rules, title-slug rules (≤60 chars, `[A-Z0-9-]`), cross-package qualifier `<package>/<id>`, kind-exhaustion as hard error. Amends DEC-002 schema v1 with optional per-package `extraKinds`. Flagged consequence: 28 occurrences of `ITEM-NN` in 6 files to be rewritten to `ITEM-NNN` on approval. |
| 2026-05-28 | `ART-DEC-004` | Approved | User approved DEC-004 on first review (no refinements). DEC-004 is now the ID-scheme foundation for DEC-005 → DEC-008, ITEM-002 (config model), ITEM-003 (ID validator). Logged as `REV-DEC-004-001` in reviews.md. Triggered the one-time `ITEM-NN → ITEM-NNN` rewrite across 6 files. Stage 0 now has 4 of 8 decisions approved; DEC-005-SKILL-INSTALLATION-MODEL is next. |
| 2026-05-28 | (multiple) | Mass rename | One-time consequence of DEC-004 approval: rewrote `ITEM-(\d{2})(?!\d)` → `ITEM-0$1` across `work_plan.md`, `ledger/artifacts.md`, `ledger/history.md`, `decisions/DEC-001`, `decisions/DEC-002`, `decisions/DEC-003`. 28 occurrences total. No items had been drafted yet, so no file renames were needed. |
| 2026-05-28 | `ART-DEC-005` | Design forks resolved | User answered structured questions (AskUserQuestion): source = files-in-repo + embedded resources; install trigger = explicit MCP tool `install_skills`; reinstall = always overwrite; boundary = user-wide skills only (project-level `agents/openai.yaml` is DEC-007 `init`). All four were the recommended options. |
| 2026-05-28 | `ART-DEC-005` | Drafted | DEC-005 drafted from Q&A answers plus filled-in mechanics: source layout under `spec/skills/<name>/SKILL.md`, canonical frontmatter (`name` + `description`, intersection of both agents' required keys), Core embedded-resource glob with `LogicalName` preserving directory structure, `IEmbeddedSkillCatalog` startup validation, `install_skills` tool contract (args: `agent`, `dryRun`; idempotent, per-agent partial-failure semantics), target Windows paths via `Environment.SpecialFolder.UserProfile`, ownership-by-name (no manifest in MVP), two new typed Core exceptions. Skill content defers to DEC-008; project-level openai.yaml defers to DEC-007. |
| 2026-05-28 | `ART-DEC-005` | Approved | User approved DEC-005 on first review (no refinements). DEC-005 is now the skill packaging/installation foundation for DEC-007 (finalizes `install_skills` envelope), DEC-008 (skill content), ITEM-004 (embedded catalog), ITEM-005 (skill installer). Logged as `REV-DEC-005-001` in reviews.md. Stage 0 now has 5 of 8 decisions approved; DEC-006-SCHEMA-VERSIONING is next. |
| 2026-05-28 | `ART-DEC-006` | Design forks resolved | User answered structured questions (AskUserQuestion): versioning style = plain integer; scope = `.specforge.json` only (with forward-looking rule for future externally-parsed formats); unsupported behavior = hard refuse + explicit error + suggested action; migration trigger = in-memory upgrade + opportunistic write. All four were the recommended options. First AskUserQuestion form came back empty; re-asked verbatim. |
| 2026-05-28 | `ART-DEC-006` | Drafted | DEC-006 drafted from Q&A answers plus filled-in mechanics: precise additive-vs-breaking rules table; authoritative Schema v1 Inventory (catalogues every v1 field with introducing-DEC reference, including the `extraKinds` additive amendment from DEC-004); `SpecforgeSchemaVersionException` fully defined (carries `Path`, `ClaimedVersion`, `SupportedRange`, `Suggestion`); explicit separation from shape-validation errors (`SpecforgeConfigValidationException`, finalized in ITEM-002); in-memory upgrade chain with opportunistic write rule; bump procedure (one DEC per bump, migration function, inventory section); read-back compatibility commitment (every binary reads every prior version for MVP era); forward-looking rule for future externally-parsed formats. |
| 2026-05-28 | `ART-DEC-006` | Approved | User approved DEC-006 on first review (no refinements). DEC-006 is now the schema-versioning policy foundation for DEC-007 (envelope-maps schema-version vs config-validation errors) and ITEM-002 (implements version check + upgrade chain + defines `SpecforgeConfigValidationException`). Logged as `REV-DEC-006-001` in reviews.md. Stage 0 now has 6 of 8 decisions approved; DEC-007-MVP-TOOL-SET is next. |
| 2026-05-28 | `ART-DEC-007` | Design forks resolved (round 1 — shape) | User answered AskUserQuestion: naming = `verb_noun` snake_case; granularity = high-level spec-graph-aware multi-effect ops; read/write split = separate tools with `dryRun` on writes; schemas = rich JSON Schema with descriptions + examples + enums. All four recommended. |
| 2026-05-28 | `ART-DEC-007` | Design forks resolved (round 2 — errors) | User answered AskUserQuestion: envelope = `{code, message, suggestion?, data?}`; codes = dotted namespaced `specforge.<domain>.<reason>`; suggestion = top-level optional field; selection-error payload = structured `data.availablePackages` + prose message + suggestion. All four recommended. Closes DEC-002 open question on selection-error shape. |
| 2026-05-28 | `ART-DEC-007` | Design forks resolved (round 3 — catalog) | User answered AskUserQuestion: status transitions = generic `set_decision_status(id, status, reviewer?, notes?)`; validation = single `validate(aspect?)`; item tools = full parity with decisions; standalone ledger appends = yes (history + review + commit). All four recommended. |
| 2026-05-28 | `ART-DEC-007` | Drafted | DEC-007 drafted from 12 Q&A answers plus filled-in mechanics: 17-tool catalog across 6 categories (setup, configuration, decisions, items, ledger, validation); full Core-exception → MCP-code mapping (12 codes); representative input-schema example for `create_decision`; explicit "what's not in MVP" list (`list_skills`, `supersede_decision`, `delete_*`, `migrate_config`, generic file CRUD, search); resolves DEC-005 OQ (`list_skills` subsumed by `install_skills dryRun=true`) and DEC-006 OQ (`info` tool exposes `supportedSchemaVersionRange`). Maps 11 planned ITEMs for Stage 1 implementation. |
| 2026-05-28 | `ART-DEC-007` | Design forks resolved (round 4 — delete tools) | User pushback on "no `delete_*` in MVP" — noted that even MVP needs undo for accidental `create_*`, typo'd ledger appends, etc. Re-opened with AskUserQuestion: scope = decisions + items + reviews + commits; allowed states = `Not started` + `Draft*` only; number policy = tombstone forever (preserves DEC-004 "numbers are never reused"); confirmation = required `confirm: true` argument. All four recommended. |
| 2026-05-28 | `ART-DEC-007` | Amended in draft | DEC-007 updated: added 4 `delete_*` tools, new error code `specforge.lifecycle.delete_forbidden`, centralized "Delete Semantics" section covering cascade rules and tombstoning, expanded Alternatives with 7 delete-related rejected options, updated catalog count 17 → 21, updated `validate(aspect=ids)` to consult `history.md` `Deleted` events for tombstone evidence. ITEM-007/008/009/010/011 mappings updated to reflect new tool ownership. |
| 2026-05-28 | `ART-DEC-007` | Approved | User approved DEC-007 after the in-draft amendment. DEC-007 is now the MCP tool surface foundation for DEC-008 (skills reference the 21 tools by name) and for ITEMs 001..011 (each tool's implementation). Logged as `REV-DEC-007-001` in reviews.md. Stage 0 now has 7 of 8 decisions approved; DEC-008-INSTRUCTION-LAYER-DESIGN is next (final Stage 0 decision). |
| 2026-05-28 | `ART-DEC-008` | Design forks resolved | User answered AskUserQuestion (Round 1): skill scope = ~6 narrow action-focused skills; project-level guidance = standalone `SPECFORGE.md` + tagged-block merge into `CLAUDE.md`/`AGENTS.md`; MCP Resources = not in MVP; shared docs = reference by relative path. All four recommended. Single round sufficed — remaining details (re-init behavior, tagged-block update model) had obvious defaults per DEC-005 always-overwrite pattern. |
| 2026-05-28 | `ART-DEC-008` | Drafted | DEC-008 drafted from Q&A answers plus filled-in mechanics: 4-layer instruction model table (tool descriptions / skills / project-level / shared docs); 6-skill MVP catalog with triggers + tools stitched + shared docs referenced; skill content principles (7 rules including no-inline-shared-docs, no-project-specific-content); `SPECFORGE.md` content shape with auto-generated sections; tagged-block convention with exact delimiters as public contract; `init` amendment adding `behavioralFiles: all\|claude-only\|codex-only\|none` argument and 3 new file outputs (`SPECFORGE.md`, `CLAUDE.md` block, `AGENTS.md` block); explicit re-init behavior per file. ITEM-006 (init) scope expanded; new ITEM-012 (skill content) and optional ITEM-013 (project-level templates) added. No new error codes. Amends DEC-007 `init` contract additively. |
| 2026-05-28 | `ART-DEC-008` | Approved | User approved DEC-008 on first review (no refinements). **Stage 0 COMPLETE: 8 of 8 decisions approved.** All Stage 0 open questions closed. Logged as `REV-DEC-008-001` in reviews.md. Next: Stage 1 (item specs) begins. |
| 2026-05-28 | (milestone) | Stage 0 complete | All eight Stage 0 decisions approved on 2026-05-28 (DEC-001 → DEC-008). Spec foundation locked: distribution (MCP stdio, self-contained Win x64), configuration (`.specforge.json` discovery), runtime (.NET 10 LTS, Core+Mcp+Tests layout), ID scheme (uniform 3-digit, additive customization), skill packaging (embedded resources, `install_skills`), schema versioning (plain integer, in-memory upgrade), MVP tool set (21 tools, structured error envelope), instruction layer (6 skills, SPECFORGE.md + tagged blocks). Stage 1 (item specs) is now unblocked. |
| 2026-05-28 | `ART-WORK-PLAN` | Updated | work_plan.md Stage 1 reconciled against Stage 0 outputs. Original 16-item draft (authored 2026-05-25 before any DEC was approved) superseded by revised 13-item list: 3 Foundation (ITEM-001..003), 2 Skill machinery (004..005), 4 Bootstrap & authoring tools (006..009), 2 Validation/envelope (010..011), 2 Content (012..013). Dependency order documented. |
| 2026-05-28 | `ART-ITEMS-README` | Status promoted | Placeholder → Draft. Populated with file-naming rules (per DEC-004), lifecycle gates (per shared/document_lifecycle.md), Stage 1 dependency order, and empty Current Items table awaiting ITEM-001. |
| 2026-05-28 | `ART-LEDGER-ITEMS` | Status promoted | Placeholder → Draft on first append (ITEM-001 entered draft). |
| 2026-05-28 | `ART-ITEM-001` | Drafted | First Stage 1 item. Specifies the .NET solution scaffold (Specforge.sln + 3 projects + Directory.Build.props + Directory.Packages.props + global.json + xUnit boundary architecture test enforcing DEC-003's Core/Mcp invariant). Sources: DEC-001 (publish path), DEC-003 (layout), DEC-007 (Mcp project hosts future tools). Zero domain logic; pure scaffolding. Iteration strategy for Stage 1: one-at-a-time with /ask only on real new forks. |
| 2026-05-28 | `ART-ITEM-001` | Approved | User approved ITEM-001 on first review (no refinements). First Stage 1 item approved; Stage 2 implementation unblocked for ITEM-001 scope. ITEM-002 (config model + 3 tools) drafted immediately after. Logged as `REV-ITEM-001-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-002` | Drafted | Vertical slice 1 of specforge functionality. Specifies: Core config model (`.specforge.json` discovery via cwd walk-up, JSON parsing, shape validation, schemaVersion check + in-memory upgrade chain, package enumeration with `extraKinds`); 5 typed Core exceptions (`SpecforgeConfigNotFoundException`, `SpecforgeConfigValidationException`, `SpecforgeSchemaVersionException`, `SpecforgePackageNotSelectedException`, `SpecforgeUnknownPackageException`); first MCP host wiring (ModelContextProtocol NuGet ref, stdio loop, tool registration pattern via `IMcpTool` interface); 3 tools (`list_packages`, `use_package`, `info`); first error envelope rendering with 5 of 13 codes. Sources: DEC-002, DEC-004 (extraKinds), DEC-006 (versioning), DEC-007 (tools + envelope). No new forks — pure consolidation of approved decisions. |
| 2026-05-28 | `ART-ITEM-002` | Approved | User approved ITEM-002 on first review (no refinements). Vertical slice 1 ready for Stage 2 implementation. ITEM-003 (ID validator) drafted immediately after. Logged as `REV-ITEM-002-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-003` | Drafted | Core-only item consolidating DEC-004 identifier rules into reusable services: `IdParser` (string → parsed Identifier), `IdValidator` (shape + composite + qualified + ART forms), `TitleSlug` (slug rules), `KindRegistry` (core 5 + per-package extraKinds with reserved-kind check), `IIdAllocator`/`IdAllocator` (next-free-number with kind-exhaustion error). 3 new typed exceptions (`SpecforgeInvalidIdentifierException`, `SpecforgeReservedKindException`, `SpecforgeKindExhaustedException`) plus their `ToolExceptionMapper` mappings. Retroactively wires the reserved-kind check into `ConfigValidator` from ITEM-002. No new forks — pure consolidation. |
| 2026-05-28 | `ART-ITEM-003` | Approved | User approved ITEM-003 on first review (no refinements). Identifier infrastructure ready for Stage 2 implementation; consumers ITEM-006/007/008/009/010 unblocked. Logged as `REV-ITEM-003-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-004` | Drafted | Core-only item realizing DEC-005's embedded-resource mechanism. Specifies: csproj `<EmbeddedResource Include="..\..\spec\skills\**\*.*" LogicalName="specforge.skills/%(RecursiveDir)%(Filename)%(Extension)" />`; first YamlDotNet NuGet introduction; `IEmbeddedSkillCatalog` + `EmbeddedSkillCatalog` (enumerate via `Assembly.GetManifestResourceNames()`); `SkillFrontmatterParser` (YamlDotNet-based extraction with `name`/`description` validation, ≤200 char description); `SkillCatalogValidator` (one-shot at MCP host bootstrap; broken skill → fail-fast server start, empty catalog OK); 1 new typed Core exception (`SpecforgeEmbeddedSkillNotFoundException`) + envelope mapping; `spec/skills/.gitkeep` created so MSBuild glob has a target. No 6 real SKILL.md files yet — that's ITEM-012. No new forks. |
| 2026-05-28 | `ART-ITEM-004` | Approved | User approved ITEM-004 on first review (no refinements). Embedded skill infrastructure ready for Stage 2 implementation; consumer ITEM-005 unblocked. Logged as `REV-ITEM-004-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-005` | Drafted | First end-to-end skill flow. Specifies: `ISkillInstaller` (Core) orchestrating catalog→agent→skill→file pipeline; `ISkillInstallTargetResolver` (Core, default uses `Environment.SpecialFolder.UserProfile`) — DI-swappable for tests to avoid touching real ~/.claude or ~/.agents; `SkillInstallAgent` enum (ClaudeCode, Codex, All); `SkillInstallResult` / `SkillInstallAgentResult` records; `install_skills` MCP tool with rich JSON Schema (agent enum + dryRun); per-agent partial-failure semantics — if any agent fails, tool returns FAILURE envelope but `data` reports both successful agents' state and the failed agent's path; 1 new typed Core exception (`SpecforgeSkillInstallException`) + envelope mapping (code `specforge.skills.install_failed`); always-overwrite reinstall semantics. No new forks. |
| 2026-05-28 | `ART-ITEM-005` | Approved | User approved ITEM-005 on first review (no refinements). First end-to-end skill flow ready for Stage 2 implementation; consumer ITEM-006 (init) and content provider ITEM-012 unblocked. Logged as `REV-ITEM-005-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-006` | Design fork resolved | User answered AskUserQuestion (single fork): re-init behavior on existing `.specforge.json` = full replacement of `packages[]` from args + in-memory upgrade chain to current schemaVersion. Recommended option. Deterministic, simple, easy to test. Incremental adds done by explicit caller passing `[old + new]`; future `add_package` tool (out of MVP) will handle incremental flows. |
| 2026-05-28 | `ART-ITEM-006` | Drafted | Biggest tool item in Stage 1. Absorbs DEC-002 config write, DEC-005 boundary (project-level `agents/openai.yaml`), DEC-006 in-memory upgrade chain, DEC-007 `init` tool contract, DEC-008 `behavioralFiles` arg + `SPECFORGE.md` + tagged-block merge into `CLAUDE.md`/`AGENTS.md`. Adds two new embedded-resource globs (`specforge.shared/**`, `specforge.templates/**`) so scaffold mode can copy canonical shared docs and templates to a fresh adopter. New Core services: `ConfigWriter`, `SpecforgeMdGenerator`, `TaggedBlockMerger`, `CodexOpenaiYamlWriter`, `IEmbeddedTemplateCatalog`, `ScaffoldEngine`, `IInitService`. New `InitTool` (5th MCP tool). No new error codes (reuses `tool.invalid_argument` for the bad-tagged-block-state case, plus existing config error codes). Tagged-block delimiters `<!-- specforge:start --> ... <!-- specforge:end -->` enforced as public contract per DEC-008. |
| 2026-05-28 | `ART-ITEM-006` | Approved | User approved ITEM-006 on first review (no refinements after the round-1 fork resolution). Init infrastructure ready for Stage 2 implementation; consumer ITEM-007 (decision tools) unblocked. Logged as `REV-ITEM-006-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-007` | Drafted | First spec-graph item — 5 decision tools (`list_decisions`, `get_decision`, `create_decision`, `set_decision_status`, `delete_decision`). Introduces ledger row primitives reused by ITEM-008/009: `LedgerRow`, `LedgerTable`, `LedgerTableParser`, `LedgerTableWriter`, plus three high-level services `IArtifactLedgerService`, `IHistoryLedgerService`, `IReviewLedgerService`. New `LifecycleStateMachine` enforcing transitions per shared/document_lifecycle.md. First Markdig NuGet introduction (used by decision document parser + ledger table parser). New `DecisionDocument` model + `DecisionDocumentParser`. 1 new typed exception (`SpecforgeDeleteForbiddenException`, code `specforge.lifecycle.delete_forbidden`) + envelope mapping (13th code). Tool count: 5 → 10. No new forks. |
| 2026-05-28 | `ART-ITEM-007` | Approved | User approved ITEM-007 on first review (no refinements). First spec-graph item ready for Stage 2 implementation; ITEM-008 (item tools) unblocked. Logged as `REV-ITEM-007-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-008` | Drafted | Item-side mirror of ITEM-007 — 5 item tools (`list_items`, `get_item`, `create_item`, `set_item_status`, `delete_item`). Reuses every infrastructure piece introduced in ITEM-007 (ledger row primitives, three ledger services, `LifecycleStateMachine`, `SpecforgeDeleteForbiddenException`, ToolExceptionMapper rows). Introduces `ItemDocument` model + Markdig-backed `ItemDocumentParser` + `ItemFileWriter` (template-substituted from `spec/templates/item_spec.md`); parser is required-section-aware per `spec/shared/spec_item_contract.md`. No new NuGet packages, no new error codes, no new globs. Tool count: 10 → 15. No new forks. |
| 2026-05-28 | `ART-ITEM-008` | Approved | User approved ITEM-008 on first review (no refinements). Item-side spec-graph tools ready for Stage 2 implementation; ITEM-009 (ledger append tools) unblocked. Logged as `REV-ITEM-008-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-009` | Drafted | Ledger-side spec-graph item — 5 ledger tools (3 standalone appends: `append_history`, `append_review`, `append_commit`; 2 deletes: `delete_review`, `delete_commit`). Introduces `ICommitLedgerService` (4th ledger service) — first writer to `<package>/ledger/commits.md` (promotes `ART-LEDGER-COMMITS` from Placeholder to Draft on first append). Reuses ITEM-007's `IHistoryLedgerService` and `IReviewLedgerService` for the other three appends. `delete_review` and `delete_commit` follow DEC-007 Delete Semantics: required `confirm: true`, tombstone forever, append `Deleted` history event. No lifecycle gate on review/commit deletes (no lifecycle states). No new NuGet packages, no new error codes, no new typed exceptions. Tool count: 15 → 20. No new forks. |
| 2026-05-28 | `ART-ITEM-009` | Approved | User approved ITEM-009 on first review (no refinements). Ledger-side spec-graph tools ready for Stage 2 implementation; ITEM-010 (validate tool — final MCP tool) unblocked. Logged as `REV-ITEM-009-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-010` | Drafted | Final MCP tool — `validate(aspect: ids\|links\|lifecycle\|impact-coverage\|all, dryRun?)`. Completes DEC-007's 21-tool catalog (tool count 20 → 21). Read-only by design (no mutations; surfaces findings only). Introduces 4 aspect validators (`IdsAspectValidator`, `LinksAspectValidator`, `LifecycleAspectValidator`, `ImpactCoverageAspectValidator`), `IValidationService` orchestrator, `TombstoneAwareIdAllocator` decorator (deferred-from-ITEM-003/007/009; consults `Deleted` history events to verify gap evidence). Structured `data.findings: [{aspect, severity, target, message, suggestion?}]` payload; severity ∈ `{error, warning, info}`. `aspect=all` runs every aspect; never short-circuits. Closes deferred concerns from ITEM-008 (required-section enforcement) and ITEM-009 (tombstone verification). 0 new NuGet packages, 0 new error codes, 0 new typed exceptions. |
| 2026-05-28 | `ART-ITEM-010` | Approved | User approved ITEM-010 on first review (no refinements). MVP MCP catalog feature-complete after this item lands (21 tools). ITEM-011 (error envelope audit/centralization) unblocked. Logged as `REV-ITEM-010-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-011` | Drafted | Centralization + audit item — no new tools. Consolidates `ToolExceptionMapper` arms accumulated incrementally across ITEM-002..010 into a single canonical surface (`SpecforgeErrorCode` static class + `IEnvelopeRenderer`). Adds invariant tests (every typed `Specforge*Exception` has a mapper arm; every code in `SpecforgeErrorCode` is documented in DEC-007 catalog and has at least one production path). Audits actual code-set against DEC-007 catalog and amends DEC-007 if any code (e.g., `specforge.id.not_found`) is in use but not catalogued. Defines the envelope JSON rendering contract `{code, message, suggestion?, data?, correlationId?}` and the `tool.internal_error` fallback semantics. 0 new tools, 0 new NuGet packages, 0 new typed exceptions (unless the audit discovers a gap). |
| 2026-05-28 | `ART-ITEM-011` | Approved | User approved ITEM-011 on first review (no refinements). Audit + centralization pass ready for Stage 2 implementation. DEC-007 amendment scheduled (13 → 14 codes; `SpecforgeIdNotFoundException` becomes 12th typed exception). ITEM-012 (skill content) unblocked. Logged as `REV-ITEM-011-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-012` | Drafted | Content-authoring item — specifies 6 SKILL.md bodies for `spec/skills/<kebab>/SKILL.md` per DEC-008 catalog (`draft-decision`, `review-decision`, `draft-item`, `impact-assessment`, `validate-spec-graph`, `adopt-existing-project`). 6 new descriptive ART rows (`ART-SKILL-*`). No code, no tools, no NuGet packages, no error codes. Skills consumed at build time by ITEM-004's `<EmbeddedResource>` glob (already in place) and at runtime by ITEM-005's `install_skills` tool (already implemented). Per-skill content section in this item gives a fresh AI session enough detail to author each SKILL.md verbatim. Includes content principles enforcement (per DEC-008's 7 rules) and an integration test that walks every skill body and asserts no tool name is mentioned that isn't in `SpecforgeErrorCode`-style registered tool list. |
| 2026-05-28 | `ART-ITEM-012` | Refined in draft | Self-review by drafter caught 4 defects + 1 design improvement before user review: (1) `draft-decision` description shortened from 209 → 192 chars; (2) `review-decision` description shortened from 204 → 177 chars; (3) line 50 forward-ref to ITEM-011 (not in Depends on) replaced with ITEM-010 + fallback paragraph; (4) tool-name regex unified to `^[a-z][a-z_]+[a-z]$` across 3 locations (was inconsistent); (5) section "Shared docs referenced" clarified to accept `spec/templates/*.md` alongside `spec/shared/*.md` (canonical outline + regex + per-skill content + test scope all now unified); bonus design improvement — single-regex tool-name check (which excludes single-word tools `init`/`info`/`validate` via the `_`-required constraint) reworked into a two-phase check (exact-match catch-all + shape-regex fallback for `_`-containing typos). Annotations on all 6 description char-counts also corrected (initial estimates were over-counted by 7-15 chars). |
| 2026-05-28 | `ART-ITEM-012` | Approved | User approved ITEM-012 after the refinement pass (no further user-requested refinements). First content-authoring item ready for Stage 2 implementation. Six SKILL.md bodies will be authored at `spec/skills/<kebab>/` from the per-skill outlines in the item spec. ITEM-013 (final docs item) unblocked. Logged as `REV-ITEM-012-001` in reviews.md. |
| 2026-05-28 | `ART-ITEM-013` | Drafted | FINAL Stage 1 item — user-facing documentation: top-level README.md revision + `docs/getting-started.md` first-time-adopter walkthrough + upgrade ritual section. 1 new descriptive ART row (`ART-DOCS-GETTING-STARTED`); revises existing `ART-README-TOP` (content change, status unchanged). 0 code, 0 tools, 0 NuGet packages, 0 error codes. Content-drift test (`DocsContentReferenceTests`) asserts every tool name + skill name mentioned in docs resolves against the live catalogs. Surfaces the project-level file outputs from DEC-008 (`.specforge.json` + `SPECFORGE.md` + tagged blocks in `CLAUDE.md`/`AGENTS.md`) + the Codex-side `agents/openai.yaml` from DEC-005, the one-time `install_skills` call, and the upgrade ritual (new binary → re-run `init` for opportunistic schema upgrade per DEC-006 → re-run `install_skills` for skill refresh → `validate(aspect=all)` for baseline). |
| 2026-05-28 | `ART-ITEM-013` | Refined in draft | Self-review by drafter caught 3 defects before user review: (1) wrong relative path in Links (`../../README.md` was 2 levels up but items→specforge-mvp→packages→spec→root needs 4 levels — fixed to `../../../../README.md`); (2) misleading verification command "Verify with `specforge.exe info`" — MCP server opens stdio loop and waits for host, doesn't print info standalone — rewritten to host-side tool-list verification; (3) style inconsistency on architecture-test reference (matched ITEM-012's `Architecture test still green` pattern without explicit ITEM-001 mention). |
| 2026-05-28 | `ART-ITEM-013` | Approved | User approved ITEM-013 after the refinement pass (no further user-requested refinements). Final Stage 1 item ready for Stage 2 implementation. Two documents (`README.md` revision + new `docs/getting-started.md`) will be authored in Stage 2. Logged as `REV-ITEM-013-001` in reviews.md. |
| 2026-05-28 | (multiple) | Catch-up | 7 descriptive ART rows added to `ledger/artifacts.md` that were committed by prior item approvals but not transcribed at the moment: 6 `ART-SKILL-*` rows for the skills planned by ITEM-012 (`ART-SKILL-DRAFT-DECISION`, `ART-SKILL-REVIEW-DECISION`, `ART-SKILL-DRAFT-ITEM`, `ART-SKILL-IMPACT-ASSESSMENT`, `ART-SKILL-VALIDATE-SPEC-GRAPH`, `ART-SKILL-ADOPT-EXISTING-PROJECT`) and 1 `ART-DOCS-GETTING-STARTED` row for `docs/getting-started.md` planned by ITEM-013. All 7 in status `Not started` — they transition to `Draft` when the corresponding files are actually authored in Stage 2. `ART-README-TOP` "Last update" cell also refreshed to record the ITEM-013-planned revision. |
| 2026-05-28 | (milestone) | Stage 1 complete | All 13 Stage 1 items approved on 2026-05-28 (ITEM-001 → ITEM-013). Spec foundation locked: scaffold (ITEM-001), config model (ITEM-002), ID validator (ITEM-003), embedded skill catalog (ITEM-004), skill installer (ITEM-005), init tool (ITEM-006), decision tools (ITEM-007), item tools (ITEM-008), ledger append tools (ITEM-009), validate tool (ITEM-010), error envelope (ITEM-011), skill content (ITEM-012), docs + getting-started (ITEM-013). 21 MCP tools across 6 categories defined. 14 error codes catalogued. 12 typed Core exceptions specified. 6 SKILL.md bodies outlined (later amended to 7 — see next event). Two user-facing documents outlined. **Stage 2 (implementation) is now unblocked.** Per `work_plan.md` Stage 2 rule: "no code merges until its item spec is Approved" — all 13 specs are now Approved, so any item's Stage 2 implementation may proceed; ITEM-001 (solution bootstrap) is the natural first item since every other item depends on it. |
| 2026-05-28 | (multiple) | Post-Stage-1 spec audit | End-to-end manual audit of the spec graph (simulating ITEM-010's `validate(aspect=all)` before code exists). Aspects checked clean: ids (8 DECs + 13 ITEMs sequential, no gaps), links (all Supersedes/Amends/Depends on/REV targets resolve), lifecycle (file Status matches ART row Status across 21 artifacts; all Approved have REV evidence), impact-coverage (all 13 items have all 16 required sections), tool-name drift (0 real drift — 12 detected items all false positives). One substantive finding surfaced: ITEM-008 mentioned `review-item` as "if added", revealing that DEC-008's skill catalog had `review-decision` but no parallel `review-item` for the items side. User chose to add `review-item` skill (catalog expansion 6 → 7). One trivial finding: `work_plan.md` "Immediate Next Step" stale from Stage 0 era. |
| 2026-05-28 | `ART-DEC-008` | Amended | Catalog amended from 6 to 7 skills by adding `review-item`. New row inserted in the "MVP Skill Catalog" table between `draft-item` and `impact-assessment`. All "six" / "6 skills" / "the six" mentions updated to seven across the file. Consequences and Related Tests sections updated. New "## Amendments" section added at the bottom recording the change. Additive amendment — existing 6 skills unchanged. DEC-008 status remains `Approved`. |
| 2026-05-28 | `ART-ITEM-012` | Amended | New per-skill outline subsection added for `review-item` at position 4 (between `draft-item` and `impact-assessment`); existing sections 4-6 renumbered to 5-7. All "six" / "6 skills" / "6-skill" / "6 × 2 = 12" mentions updated to seven / 7 / 7-skill / 7 × 2 = 14 across the file. Handoff Summary updated to mention 7 SKILL.md bodies + amendment context. Updates ledger rows field amended to include `ART-SKILL-REVIEW-ITEM`. Done Criteria updated. Validation criteria updated. Item status remains `Approved`. |
| 2026-05-28 | `ART-ITEM-008` | Amended | Problem Slice line 27 updated: "`review-item` if added" replaced with concrete reference noting catalog amendment. Item status remains `Approved`. |
| 2026-05-28 | `ART-ITEM-013` | Amended | Three updates: (1) skill-catalog count in content-drift test description updated from 6 → 7 with `review-item` added to the enumerated list; (2) "Skill-name drift" validation entry updated to reference 7-skill catalog; (3) "Author your first item" section in docs/getting-started.md scope now references both `draft-item` and `review-item` skills with explicit approval walkthrough. Item status remains `Approved`. |
| 2026-05-28 | `ART-SKILL-REVIEW-ITEM` | Created | Planned per ITEM-012 amendment. `Not started` until Stage 2 authoring. Mirrors `ART-SKILL-REVIEW-DECISION` structurally; per-skill outline lives in ITEM-012 §4. |
| 2026-05-28 | `ART-WORK-PLAN` | Updated | "Immediate Next Step" section refreshed: was "Stage 0 complete (2026-05-28). Stage 1 begins with ITEM-001..." (stale from Stage 0 → Stage 1 transition); now reflects Stage 1 complete + Stage 2 begins with ITEM-001 (which remains the natural first item per dependency order). |
| 2026-05-29 | (milestone) | Stage 2 begins | First implementation code committed. `git init` ran at `D:\Work\specforge` (no repo existed before); identity ArieSLV / <user>@ravendb.net; default branch `master`. Initial commit `5bea084` carries the full Stage 0/1 spec plus ITEM-001's solution scaffold. RavenDB's `RavenDB-#### ` commit convention does NOT apply — specforge is a separate repo with its own ID-referencing message style. |
| 2026-05-29 | `ART-LEDGER-COMMITS` | Status promoted | Placeholder → Draft on first implementation commit (CMT-001). |
| 2026-05-29 | `ART-ITEM-001` | Implemented | Stage 2 implementation of ITEM-001 complete. All 6 Done Criteria met: (1) all Code Scope files exist; (2) `dotnet build Specforge.sln -c Release` → 0 warnings, 0 errors on .NET 10.0.204; (3) `dotnet test` → 2 boundary tests pass; (4) publish → `D:\Work\specforge\bin\specforge.exe` (73.4 MB, win-x64 self-contained single-file, launches with exit 0); (5) CMT-001 recorded; (6) this event. Implementation notes: classic `.sln` chosen over default `.slnx` to match spec; `AssemblyMarker.cs` serves as the Core placeholder + boundary-test anchor; inherited `MSBuildSDKsPath` env var (from RavenDB toolchain, pinned 8.0.403) had to be cleared per build invocation so global.json's .NET 10 pin takes effect. Test Plan §3 deliberate-negative-test executed: temporarily referenced `Microsoft.Extensions.Hosting` from Core → `SpecforgeCore_ReferencesNo_HttpHosting` failed as designed → reverted, green again. |
| 2026-05-29 | `CMT-001` | Created | Bootstrap commit `5bea084` on branch `master`, linked to `ART-ITEM-001`. First row in the commit ledger. |
| 2026-05-29 | `ART-ITEM-002` | Implemented | Stage 2 implementation of ITEM-002 complete. All 8 Done Criteria met: (1) all Code Scope files exist (Core `Configuration/*`, `Diagnostics/BinaryInfo`, 5 typed exceptions; Mcp `Hosting/*` + `Tools/*`; 8 test classes); (2) `dotnet build Specforge.sln -c Release` → 0 warnings; (3) `dotnet test` → 35 pass (33 new + 2 ITEM-001 boundary); (4) publish → `bin/specforge.exe` (76 MB) genuinely speaks MCP; (5) dogfood `.specforge.json` at repo root (schemaVersion 1, single package `specforge-mvp`); (6) smoke evidence `test/Specforge.Tests/Evidence/itm002-smoke.txt` — real `initialize` + `tools/call info` exchange returns `activePackage=specforge-mvp`, logs on stderr only; (7) CMT-002 recorded; (8) this event. Notes: MCP SDK pinned to `ModelContextProtocol.Core` 1.3.0 (low-level surface — no `Microsoft.Extensions.Hosting`, keeping DEC-003's Mcp dependency list honest); host wires `McpServerOptions.Handlers` (ListTools/CallTool) + `McpServer.Create` over `StdioServerTransport`; tool payloads delivered as a JSON text content block. Two additive helpers not in the Code Scope list (`SpecforgeWorkingDirectory`, `SpecforgeToolBase`) factor cwd + once-per-session load so tools are testable in-proc against fixture dirs. Versions: `Microsoft.Extensions.*` 10.0.8; Markdig 1.2.0 / YamlDotNet 18.0.0 pre-staged in `Directory.Packages.props` for ITEM-004/007. Boundary test still green. |
| 2026-05-29 | `CMT-002` | Created | Implementation commit `87228a0` on branch `master`, linked to `ART-ITEM-002`. |
| 2026-05-29 | `ART-ITEM-003` | Implemented | Stage 2 implementation of ITEM-003 complete (Core-only, no new MCP tool). All 8 Done Criteria met: (1) all Code Scope files exist — `Identifiers/{ParsedIdentifier, IdRegexes, IdParser, TitleSlug, KindRegistry, ValidationContext, IdValidator, IIdAllocator, IdAllocator, LedgerReader}` + 3 exceptions; (2) build 0 warnings; (3) `dotnet test` → 114 pass (79 new); (4) Core carries no ModelContextProtocol ref (boundary test green); (5) reserved-kind retrofit active — `ConfigValidator.EnforceReservedKinds` (called by `ConfigLoader`) rejects `extraKinds:["DEC"]` at load time; (6) dogfood — every repo ledger identifier parses (`IdParserTests.Dogfood_RepoIdentifiers_Parse`); (7) CMT-003; (8) this event. Notes: 4 forms parsed via `[GeneratedRegex]` (atomic / composite REV / descriptive ART / qualified); allocator selects ledger file by kind (artifacts.md for DEC/ITEM/extra mirroring rows, commits.md for CMT, reviews.md for REV) → max+1, throws at 999; `IIdAllocator` interface-first so ITEM-010's `TombstoneAwareIdAllocator` can decorate it. 3 new `ToolExceptionMapper` arms (`specforge.id.invalid` / `.kind_reserved` / `.kind_exhausted`). Two helpers beyond the Code Scope list: `ValidationContext` (qualified-package resolution input) and the `ILedgerReader` interface co-located in `LedgerReader.cs`. |
| 2026-05-29 | `CMT-003` | Created | Implementation commit `34e0868` on branch `master`, linked to `ART-ITEM-003`. |
| 2026-05-29 | `ART-ITEM-004` | Implemented | Stage 2 implementation of ITEM-004 complete (Core-only, no new MCP tool). All 10 Done Criteria met: Code Scope files exist (`Skills/{EmbeddedSkill, EmbeddedSkillFile, SkillFrontmatter, SkillFrontmatterParser, IEmbeddedSkillCatalog, EmbeddedSkillCatalog, SkillCatalogValidator}` + `SpecforgeEmbeddedSkillNotFoundException`); `Specforge.Core.csproj` embeds `spec/skills/**` (LogicalName `specforge.skills/...`, `.gitkeep` excluded) + references YamlDotNet 18.0.0; `spec/skills/.gitkeep` anchors the dir; build 0 warnings; `dotnet test` → 131 pass (16 new); boundary test green; empty-catalog startup verified via `info` smoke; broken-skill startup failure captured (`itm004-startup-fail.txt`, exit 1). 11th error code `specforge.skills.catalog_missing` wired into `ToolExceptionMapper`; startup validator in `Program.cs` fails the server start on a broken catalog. **Bug found + fixed during evidence run:** MSBuild `%(RecursiveDir)` emits `\` on Windows, so production logical names are `specforge.skills/<dir>\SKILL.md`; the catalog now normalizes `\`→`/` before structural parsing (keeps the original name for stream access). Regression test `EmbeddedSkillCatalogTests.BackslashLogicalName_IsNormalized`. CMT-004. |
| 2026-05-29 | `CMT-004` | Created | Implementation commit `c6e33c7` on branch `master`, linked to `ART-ITEM-004`. |
| 2026-05-29 | `ART-ITEM-005` | Implemented | Stage 2 implementation of ITEM-005 complete. All 8 Done Criteria met: Code Scope files exist (`Skills/{SkillInstallAgent, SkillInstallResult, SkillInstallAgentResult, ISkillInstaller, SkillInstaller, ISkillInstallTargetResolver, SkillInstallTargetResolver}` + `SpecforgeSkillInstallException`; `Tools/InstallSkillsTool`); build 0 warnings; `dotnet test` → 146 pass (16 new); boundary green; **4 MCP tools** registered (`list_packages`, `use_package`, `info`, `install_skills`); both evidence transcripts present (`itm005-smoke.txt` empty-catalog dryRun → zero counts, `itm005-partial-failure.txt`). 12th error code `specforge.skills.install_failed` with partial-state `data.partialResult`. Installer is byte-for-byte / always-overwrite / no-rollback on partial failure; `ISkillInstallTargetResolver` (UserProfile-based, DI-swappable) keeps tests off the real home dir. Extended ITEM-004's `EmbeddedSkill` with a `SkillMarkdown` member so the installer can write the SKILL.md itself (`Files` still excludes it). CMT-005. |
| 2026-05-29 | `CMT-005` | Created | Implementation commit `11f002e` on branch `master`, linked to `ART-ITEM-005`. |
| 2026-05-29 | `ART-ITEM-006` | Implemented | Stage 2 implementation of ITEM-006 complete (largest tool item). All 9 Done Criteria met: Code Scope files exist (`Init/{InitOptions, InitPackageInput, InitResult, InitSectionResult, BehavioralFilesScope, ConfigWriter, SpecforgeMdGenerator, TaggedBlockMerger, CodexOpenaiYamlWriter, EmbeddedTemplate, IEmbeddedTemplateCatalog, EmbeddedTemplateCatalog, ScaffoldEngine, IInitService, InitService}` + `SpecforgeInvalidArgumentException`; `Tools/InitTool`); two new embedded-resource globs (`spec/shared/**`, `spec/templates/**`, scoped so they never double-embed skills); build 0 warnings; `dotnet test` → 178 pass (32 new); boundary green; **5 MCP tools** (`list_packages`, `use_package`, `info`, `install_skills`, `init`); three evidence transcripts present. `init` writes `.specforge.json` (atomic temp-rename, canonical JSON, re-init full `packages[]` replacement), `SPECFORGE.md` (always-overwrite, DEC-008 sections), tagged blocks in `CLAUDE.md`/`AGENTS.md` (replace/prepend/create; imbalance → `invalid_argument`), and `agents/openai.yaml` (always, DEC-005 boundary). `behavioralFiles` scope (all/claude-only/codex-only/none) + non-destructive scaffold honored; `dryRun` writes nothing. Idempotency proven against the published binary (init ×2 → byte-identical, SHA tree hashes equal). Note: added `SpecforgeInvalidArgumentException` carrier + mapper arm (DEC-008 said "reuse `invalid_argument` semantics"; a typed carrier is the cleanest way for deep Core code to surface the code; ITEM-011 will consolidate). CMT-006. |
| 2026-05-29 | `CMT-006` | Created | Implementation commit `a7e616a` on branch `master`, linked to `ART-ITEM-006`. |
| 2026-05-29 | `ART-ITEM-007` | Implemented | Stage 2 implementation of ITEM-007 complete (first spec-graph item). All 9 Done Criteria met: Code Scope files exist (`Ledger/*` row primitives + line-surgical `LedgerDocument` + 3 ledger services + `LedgerPaths`, `Lifecycle/{LifecycleState, LifecycleStateMachine}`, `Documents/{DecisionDocument, DecisionDocumentParser, DecisionFileWriter, DecisionService, DecisionResults}`, `SpecforgeDeleteForbiddenException`; Markdig added to Core; `Tools/Decisions/*` ×5); build 0 warnings; `dotnet test` → 217 pass (39 new); boundary green (Markdig confined to Core); **10 MCP tools**; 13th code `specforge.lifecycle.delete_forbidden`. create/set/delete are multi-effect (decision file + ART row + history + REV cascade); lifecycle transition gate + `confirm:true` gate enforced; ledger writes are line-surgical (minimal diff) + atomic temp-rename. Evidence: itm007-dogfood-parse.txt (8 repo DEC files parsed via the published binary) + itm007-create-delete.txt. Notes: ledger-table parsing is line-based (not Markdig) for byte-stable minimal-diff row surgery — Markdig backs the decision *document* section parser; the shared `document_lifecycle.md` predates the finalized state vocabulary, so `LifecycleStateMachine` follows ITEM-007 §6 / DEC-007 (a known shared-doc drift to reconcile in a later cleanup). CMT-007. |
| 2026-05-29 | `CMT-007` | Created | Implementation commit `daac843` on branch `master`, linked to `ART-ITEM-007`. |
| 2026-05-30 | `ART-ITEM-008` | Implemented | Stage 2 implementation of ITEM-008 complete (item-side mirror of ITEM-007). All 9 Done Criteria met: Code Scope files exist (`Documents/{ItemDocument, ItemDocumentParser, ItemFileWriter, ItemResults, ItemService, RequiredItemSections}`; `Tools/Items/*` ×5); build 0 warnings; `dotnet test` → 238 pass (21 new); boundary green; **15 MCP tools**; no new error codes / NuGet / globs. Reuses ITEM-007's ledger primitives, 3 ledger services, `LifecycleStateMachine`, `SpecforgeDeleteForbiddenException`. Ledger routing implemented: `ArtifactLedgerService` routes `ART-ITEM-*` rows to `ledger/items.md` and `ART-DEC-*`/others to `ledger/artifacts.md`; `IdAllocator` scans `items.md` for the `ITEM` kind to match. `RequiredItemSections` (12 required-for-Approved, 16 full template) with a contract-drift test against `spec_item_contract.md`. `create_item` validates `dependsOn` shape/kind (not existence). Binary evidence: itm008-dogfood-parse.txt (13 repo items) + itm008-create-delete.txt (full serial create→set→list→delete round-trip; history Created/Draft for user review/Deleted). CMT-008. |
| 2026-05-30 | `CMT-008` | Created | Implementation commit `cfb5b09` on branch `master`, linked to `ART-ITEM-008`. |
| 2026-05-30 | `ART-ITEM-009` | Implemented | Stage 2 implementation of ITEM-009 complete (standalone ledger append + delete tools). All 9 Done Criteria met: Code Scope files exist (`Ledger/{HistoryLiteralTarget, ICommitLedgerService, CommitLedgerService}` + `IHistoryLedgerService.AppendLiteralAsync` overload + `HistoryLedgerService` impl; `Tools/Ledger/*` ×5); build 0 warnings; `dotnet test` → 279 pass (41 new); boundary green; **20 MCP tools**. 5 tools: `append_history` (LedgerId OR closed literal set `(milestone)`/`(multiple)`), `append_review` (REV per-target seq + Reviewed event, free-form outcome), `append_commit` (CMT global seq + Implemented event + first-call `ART-LEDGER-COMMITS` Placeholder→Draft promotion), `delete_review`/`delete_commit` (confirm:true gate, NO lifecycle gate, tombstone never reused). `ICommitLedgerService`/`CommitLedgerService` = 4th ledger service + first writer to commits.md (tombstone-tolerant max+1; owns the promotion side-effect). `CommitRow` now renders Linked/Branch as code-spans to match the dogfood style. Not-found surfaced INLINE as `specforge.id.not_found` (NO new typed exception, NO new mapper arm) per the ITEM-011 audit plan (REV-ITEM-011-001) — `SpecforgeToolBase` gains `IdNotFound`/`ConfirmRequired`/`ToArtifactId`/`BareTarget`. 0 new NuGet, 0 new catalog codes, 0 new globs. `append_commit` resolves OQ-1 (optional `date` arg). Binary evidence: itm009-append-cycle.txt (literal+LedgerId history, review, commit + Placeholder→Draft promotion end-to-end) + itm009-delete-cycle.txt (tombstone deletes + REV-…-003/CMT-003 seq advancement + confirm gate). CMT-009. |
| 2026-05-30 | `CMT-009` | Created | Implementation commit `db9135e` on branch `master`, linked to `ART-ITEM-009`. |
| 2026-05-30 | `ART-ITEM-010` | Implemented | Stage 2 implementation of ITEM-010 complete (final MVP tool — DEC-007 catalog now 21/21). All 10 Done Criteria met: Code Scope files exist (`Validation/{ValidationFinding, ValidationResult, TombstoneDetailFormat, TombstoneAwareIdAllocator, IIdsAspectValidator+IdsAspectValidator, ILinksAspectValidator+LinksAspectValidator, ILifecycleAspectValidator+LifecycleAspectValidator, IImpactCoverageAspectValidator+ImpactCoverageAspectValidator, IValidationService+ValidationService}` + internal `SpecGraphScan`; `Tools/Validation/ValidateTool`); build 0 warnings; `dotnet test` → 318 pass (39 new); boundary green; **21 MCP tools**. `validate(aspect=ids|links|lifecycle|impact-coverage|all)` is read-only, findings-as-data (SUCCESS even with errors), no short-circuit on `all`. Locks the tombstone-detail contract `Tombstone <id>: <rest>` and RETROFITS all 4 delete tools (delete_decision/item/review/commit) to call `TombstoneDetailFormat.Format`. `TombstoneAwareIdAllocator` decorator adds `VerifyGapsAsync` (only untombstoned gaps are errors). **Dogfood cleanup found by the new validator: removed 13 stale `ART-ITEM-*` rows from `artifacts.md` (ITEM-008 routing means they live only in `items.md`; the duplicates predated the migration).** `DogfoodValidationTests.ValidateAll_LiveSpecGraph_HasNoErrors` is green (errorCount=0). 0 new NuGet / error codes / typed exceptions / globs. Evidence: itm010-dogfood-validate-all.txt (clean) + itm010-sad-path.txt (one finding per category). CMT-010. |
| 2026-05-30 | `CMT-010` | Created | Implementation commit `b007634` on branch `master`, linked to `ART-ITEM-010`. |
| 2026-05-30 | `ART-ITEM-011` | Implemented | Stage 2 implementation of ITEM-011 complete (error-envelope centralization + audit). All 13 Done Criteria met: Code Scope files exist (`Diagnostics/SpecforgeErrorCode`, `Exceptions/SpecforgeIdNotFoundException`, `Errors/{MappedError, ToolExceptionMapper, IEnvelopeRenderer, EnvelopeRenderer}`; `McpErrorEnvelope` serves the EnvelopeJson role); build 0 warnings; `dotnet test` → 326 pass; boundary green; **21 MCP tools** (unchanged). Single const store (14 codes); all 13 typed exceptions set ErrorCode from it (Core literal-free); consolidated mapper (one MappedError arm per type + `HandledTypes`); `EnvelopeRenderer` is the sole envelope builder (correlationId+stderr for `tool.internal_error`; static `InvalidArgument`). `delete_review`/`delete_commit` + `append_review`/`append_commit` now throw `SpecforgeIdNotFoundException` (was inline). 5 invariant tests (ErrorCode shape/count, EveryTypedExceptionHasMapperArm, CatalogDrift via LedgerTableParser, OrphanCode, NoStringLiteralCodes) + EnvelopeRendererTests (all 14 codes). DEC-007 amended additively 13→14. **Spec drift noted: ITEM-011 §inventory omitted SpecforgeInvalidArgumentException, so the real typed-exception count is 13, not the spec's "12"; AllCodes is 14 as specified.** 0 new NuGet, 0 new tools. Evidence: itm011-catalog-render.txt + itm011-correlation-id.txt. CMT-011. |
| 2026-05-30 | `CMT-011` | Created | Implementation commit `b0a8d0e` on branch `master`, linked to `ART-ITEM-011`. |
| 2026-05-30 | `ART-DEC-007` | Amended | DEC-007 catalog amended by ITEM-011 to add `specforge.id.not_found` (13 → 14 codes); now sourced from the typed `SpecforgeIdNotFoundException`. Structurally additive — no existing code's contract changed. |
| 2026-05-30 | `ART-ITEM-012` | Implemented | Stage 2 implementation of ITEM-012 complete (skill content). All 12 Done Criteria met: 7 SKILL.md bodies authored at `spec/skills/<kebab>/SKILL.md`; each frontmatter validated (name=dir, description ≤200); canonical body outline; `SkillContentReferenceTests` (3 theories × 7 + file-existence) — tool-name two-phase check vs the 21-tool catalog, shared-doc/template path resolution, cross-skill names ∈ 7-skill set. Embedded by ITEM-004's glob (no csproj change); deployed by ITEM-005's `install_skills` (dryRun=all plans 14 paths = 7×2). 0 code/tools/NuGet/codes/globs. Build 0 warnings; 348 tests pass (22 new). Evidence itm012-install-skills.txt + itm012-skill-bodies.txt. CMT-012. |
| 2026-05-30 | `CMT-012` | Created | Implementation commit `5633a69` on branch `master`, linked to `ART-ITEM-012`. |
| 2026-05-30 | `ART-SKILL-DRAFT-DECISION` | Created | `spec/skills/draft-decision/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-REVIEW-DECISION` | Created | `spec/skills/review-decision/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-DRAFT-ITEM` | Created | `spec/skills/draft-item/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-REVIEW-ITEM` | Created | `spec/skills/review-item/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-IMPACT-ASSESSMENT` | Created | `spec/skills/impact-assessment/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-VALIDATE-SPEC-GRAPH` | Created | `spec/skills/validate-spec-graph/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-SKILL-ADOPT-EXISTING-PROJECT` | Created | `spec/skills/adopt-existing-project/SKILL.md` authored (ITEM-012); promoted Not started → Draft. |
| 2026-05-30 | `ART-ITEM-013` | Implemented | Stage 2 implementation of ITEM-013 complete (user-facing docs — the FINAL item). All Done Criteria met: `docs/getting-started.md` exists with every Target-Behavior-§2 section (Before you start; Install build-from-source + pre-built; Register MCP server `.mcp.json` + `agents/openai.yaml`; bootstrap quartet init→install_skills→use_package→validate; Author your first decision; Author your first item; Validate with severity meanings; Undo a mistaken creation; Upgrade ritual; Where to learn more); `README.md` revised per Target Behavior §1 (tagline, what-is with 21 tools across 5 families, quick start, requirements, docs links, License placeholder, single-sentence Status); `DocsContentReferenceTests` (3 theories + 2 facts) green; build 0 warnings; `dotnet test` → 356 pass (8 new); boundary green; **21 MCP tools** unchanged (content-only item — no Core/Mcp code). 0 new NuGet / codes / exceptions / globs. Authoring caught a real `codex-only` enum false-positive via the skill-name drift guard (fixed by combined-span rendering); a self-review agent returned PASS WITH NITS and the one SHOULD-FIX (README publish-command caption over-claimed "self-contained" relative to the bare one-liner) was fixed. Binary evidence: itm013-bootstrap-walkthrough.txt (fresh temp-dir quartet; init wrote 4 behavioral + `agents/openai.yaml` + 17 scaffold files; install_skills dryRun planned 14 paths; use_package auto-selected; validate errorCount=0) + itm013-upgrade-walkthrough.txt (re-init against the bootstrapped fixture: config+behavioral overwritten, scaffold non-destructively skipped, validate errorCount=0; schema upgrade a documented no-op at supportedSchemaVersionRange [1,1]). install_skills used dryRun=true in both transcripts to spare the real user profile (byte-level writes covered by ITEM-005 unit tests). CMT-013. |
| 2026-05-30 | `CMT-013` | Created | Implementation commit `55e2cd3` on branch `master`, linked to `ART-ITEM-013`. |
| 2026-05-30 | `ART-DOCS-GETTING-STARTED` | Created | `docs/getting-started.md` authored (ITEM-013): the human-facing onboarding walkthrough (install → register → bootstrap quartet → author first decision/item → validate → upgrade ritual). Promoted Not started → Draft. The AI-facing counterpart is the `adopt-existing-project` skill (ITEM-012). |
| 2026-05-30 | `ART-README-TOP` | Revised | Top-level `README.md` content revised by ITEM-013 (tagline, "What is specforge?", quick start with install + `.mcp.json` snippet, requirements, documentation links to `docs/getting-started.md` + decisions/items/shared, License placeholder, single-sentence Status). Lifecycle unchanged (Draft); content change recorded here per the ledger convention (no new ART row for an in-place revision). |
| 2026-05-30 | `(milestone)` | Implemented | **Stage 2 implementation complete — all 13 of 13 items implemented (ITEM-001..ITEM-013).** specforge MVP is a runnable self-contained Windows x64 MCP binary: 21 tools across 5 families (setup/decisions/items/ledger/validate), 14 error codes, 13 typed Core exceptions, 7 embedded+installable skills, read-only spec-graph validation. Dogfood `validate(aspect=all)` clean (errorCount=0). 356 tests pass. Next: Stage 3 (first external adoption). |
| 2026-06-04 | `ART-LICENSE` | Created | `LICENSE` added — MIT License (post-Stage-2 repo hygiene); promoted Not started → Draft. README "License" section updated to reference it. Copyright holder line defaults to the author and is editable. |
| 2026-06-04 | `ART-CI-WORKFLOW` | Created | `.github/workflows/ci.yml` added — GitHub Actions CI (windows-latest; restore → build Release [0-warning gate via TreatWarningsAsErrors] → `dotnet test` incl. the dogfood validate → publish smoke); promoted Not started → Draft. To be wired once the GitHub remote is configured. |
| 2026-06-04 | `ART-README-TOP` | Revised | README revised (post-Stage-2 hygiene): License section now MIT (links `LICENSE`); the MCP-registration quick start now leads with `claude mcp add` (Claude Code CLI), keeping the manual `.mcp.json` form as a fallback. |
| 2026-06-04 | `ART-DOCS-GETTING-STARTED` | Revised | "Register the MCP server" section reconciled to lead with `claude mcp add specforge --scope user` (version-stable; Claude Code stores it in `~/.claude.json`); manual `.mcp.json` kept as fallback. Reflects real-host validation on Claude Code 2.1.150. |
| 2026-06-04 | `ART-DEC-001` | Note | **Real-host validation finding (NOT yet a DEC amendment):** DEC-001 documents MCP registration at `~/.claude/.mcp.json`, but current Claude Code (2.1.150) registers via `claude mcp add` into `~/.claude.json` — `~/.claude/.mcp.json` does not exist on a real machine. User-facing docs (README + getting-started) were reconciled; DEC-001 itself should be amended through the decision flow (`set_decision_status` round-trip) to record `claude mcp add` as the canonical registration method. Tracked as a Stage-3 input. |
| 2026-06-04 | `(milestone)` | Reviewed | **Real-host validation passed on Claude Code 2.1.150**: `claude mcp add` registered specforge (existing servers preserved); `claude mcp list` → specforge ✓ Connected (real stdio handshake by the host); `tools/list` → 21 tools; real `install_skills agent=all` wrote 14 SKILL.md (7 × claude-code + 7 × codex) into `~/.claude/skills` + `~/.agents/skills`, and the host loaded the seven skills. |
| 2026-06-04 | `(milestone)` | Note | **Git history rewritten for privacy (git-filter-repo, two passes).** Scrubbed a personal username from evidence files and this ledger (replaced with `<user>`), scrubbed the display name from all file contents (replaced with `ArieSLV`, including the `LICENSE` copyright line), and remapped every commit's author/committer to `ArieSLV <ArieSLV@users.noreply.github.com>`. All commit SHAs changed; the CMT-001..CMT-013 SHAs recorded across commits.md / items.md / artifacts.md / work_ledger.md / this file were reconciled to the post-rewrite values via the filter-repo commit-map (each verified reachable from HEAD). Repo git identity (`user.name`/`user.email`) set to ArieSLV for subsequent commits. Force-pushed to origin (sole consumer; no other clones existed). |
