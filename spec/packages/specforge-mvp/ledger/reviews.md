# Review Ledger — specforge-mvp

Status: Draft

Append-only review events: user feedback, architect approvals, requested changes.

| LedgerId | Target | Reviewer | Outcome | Date | Notes |
|---|---|---|---|---|---|
| `REV-DEC-001-001` | `ART-DEC-001` | User | Approved | 2026-05-25 | Approved after iterations: publish-path made explicit with `-o` flag, multi-agent scope refined (Claude Code + Codex CLI in scope; Cursor explicitly out), Codex skills mechanism researched against OpenAI 2026 documentation and confirmed structurally aligned with Claude Code SKILL.md format. |
| `REV-DEC-002-001` | `ART-DEC-002` | User | Approved | 2026-05-28 | Approved after structured-Q&A-driven design: discovery via `.specforge.json` with cwd walk-up; explicit `packages: [{name, path}]` (not glob) for portability to non-conforming layouts; single-package auto-select / multi-package requires `use_package`; file-level whole-file override of shared/templates; override is convention-only (filesystem is the single source of truth, no config field); no `defaultPackage` field in MVP; selection-error payload formally deferred to DEC-007. |
| `REV-DEC-003-001` | `ART-DEC-003` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Locks: .NET 10 LTS, three-project layout (Specforge.Core + Specforge.Mcp + Specforge.Tests in one solution), self-contained single-file Win x64 publish via `dotnet publish ... -o D:\Work\specforge\bin\`, AssemblyName=`specforge` so DEC-001's `.mcp.json` path matches, architecture-test-enforced Core/Mcp boundary, baseline libraries (Markdig, YamlDotNet, ModelContextProtocol, xUnit), stderr-only logging convention, typed Core exceptions with MCP envelope mapping deferred to DEC-007. |
| `REV-DEC-004-001` | `ART-DEC-004` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Locks: uniform 3-digit IDs across all kinds; core kinds `{DEC, ITEM, ART, REV, CMT}` fixed; additive per-package `extraKinds` (extends DEC-002 schema v1, backward-compatible); composite `REV-<target>-<seq>`; sequential `CMT-NNN` with separate `git-ref` column; HIST not a kind (composite key); two-flavor `ART-` form (mirroring vs descriptive standalone); identifier immutability after first non-`Not started` status; cross-package qualifier `<package>/<id>`; range 001..999 with kind-exhaustion as hard error; title slug `[A-Z0-9-]` ≤60 chars. New typed Core exceptions: `SpecforgeReservedKindException`, `SpecforgeKindExhaustedException`, `SpecforgeInvalidIdentifierException`. Triggered one-time rename of 28 `ITEM-NN` occurrences across 6 files. |
| `REV-DEC-005-001` | `ART-DEC-005` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Locks: skill source at `spec/skills/<kebab-name>/SKILL.md`; canonical frontmatter intersection (`name` + `description`); build-time embedding into `Specforge.Core.dll` via `<EmbeddedResource>` with `LogicalName="specforge.skills/%(RecursiveDir)..."`; `IEmbeddedSkillCatalog` startup validation; explicit `install_skills` MCP tool (args `agent` ∈ `{claude-code, codex, all}`, `dryRun`); always-overwrite reinstall semantics with no manifest; target Windows paths via `Environment.SpecialFolder.UserProfile`; ownership-by-name (no namespacing); strict user-wide scope (project-level `agents/openai.yaml` is DEC-007 `init`); skill version = binary version. New typed Core exceptions: `SpecforgeSkillInstallException`, `SpecforgeEmbeddedSkillNotFoundException`. |
| `REV-DEC-006-001` | `ART-DEC-006` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Locks: plain-integer `schemaVersion` (no semver/date); scope rule "what specforge parses externally" (currently `.specforge.json` only); precise additive-vs-breaking definitions; authoritative Schema v1 Inventory listing every v1 field with its introducing DEC (including DEC-004's `extraKinds` additive amendment); hard-refuse on unsupported versions via fully-defined `SpecforgeSchemaVersionException` (`Path`, `ClaimedVersion`, `SupportedRange`, `Suggestion`); explicit separation from shape-validation errors (`SpecforgeConfigValidationException`, finalized in ITEM-002); in-memory upgrade chain v_M → v_N with opportunistic write (never rewrite on pure reads); bump procedure (one DEC per bump, migration function, inventory section, rationale); MVP-era commitment to read every prior version with future deprecation policy trigger at chain > 5 entries. |
| `REV-DEC-007-001` | `ART-DEC-007` | User | Approved | 2026-05-28 | Approved after one round of refinement. Locks: 21-tool MVP catalog across 6 categories (setup, configuration, decisions, items, ledger, validation); `verb_noun` snake_case naming; high-level spec-graph-aware multi-effect ops; separate read/write tools with `dryRun`; rich JSON Schema with descriptions + examples + enums; structured `{code, message, suggestion?, data?}` error envelope with dotted-namespaced codes; 13-row Core-exception → MCP-code mapping table; centralized Delete Semantics (`delete_decision`/`delete_item`/`delete_review`/`delete_commit` added after user pushback on initial no-delete exclusion — pre-Approved only, tombstone forever, cascade-to-reviews, required `confirm: true`); `specforge.lifecycle.delete_forbidden` error code; tombstone-aware `validate(aspect=ids)`. Closes 3 prior OQs: DEC-002 selection-error payload (`data.availablePackages`), DEC-005 list_skills subsumed by `install_skills dryRun=true`, DEC-006 `info` tool exposes `supportedSchemaVersionRange`. Resolved during review: addition of all four `delete_*` tools via 4th Q&A round (4 questions, all recommended). |
| `REV-DEC-008-001` | `ART-DEC-008` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Final Stage 0 decision. Locks: 4-layer instruction model (tool descriptions / skills / project-level / shared docs); 6 MVP skills (`draft-decision`, `review-decision`, `draft-item`, `impact-assessment`, `validate-spec-graph`, `adopt-existing-project`) with triggers and tool-stitches enumerated; 7 skill content principles (frontmatter intersection, imperative voice, exact tool names, shared-docs-by-relative-path, no inline schemas, explicit cross-skill references, no project-specific facts in skills); `SPECFORGE.md` content shape with auto-generated sections; tagged-block convention `<!-- specforge:start --> ... <!-- specforge:end -->` as public contract for `CLAUDE.md`/`AGENTS.md`; no MCP Resources in MVP; shared docs surfaced by relative-path reference (no specforge wrapper). Amends DEC-007 `init` with `behavioralFiles: all\|claude-only\|codex-only\|none` arg and 3 new file outputs (SPECFORGE.md + 2 tagged blocks); no new error codes. Stage 0 now complete: 8 of 8. |
| `REV-ITEM-001-001` | `ART-ITEM-001` | User | Approved | 2026-05-28 | Approved on first review (no refinements). First Stage 1 item approved. Locks scaffold: Specforge.sln + 3 projects (Specforge.Core, Specforge.Mcp with AssemblyName=specforge, Specforge.Tests) + Directory.Build.props (TWAE/Nullable/ImplicitUsings) + Directory.Packages.props (CPM) + global.json (.NET 10 LTS rollForward=latestFeature) + xUnit boundary architecture test enforcing zero MCP/Hosting refs in Core. Zero domain logic; pure scaffolding ready for Stage 2 implementation. |
| `REV-ITEM-002-001` | `ART-ITEM-002` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Vertical slice 1: Core config model (`.specforge.json` discovery via cwd walk-up, JSON parsing, shape validation, schemaVersion check + in-memory upgrade chain, package enumeration with `extraKinds` shape check); 5 typed Core exceptions (`SpecforgeConfigNotFoundException`, `ConfigValidationException`, `SchemaVersionException`, `PackageNotSelectedException`, `UnknownPackageException`); first MCP host wiring with `ModelContextProtocol` SDK; `IMcpTool` registration pattern reused by ITEM-005..010; 3 tools (`list_packages`, `use_package`, `info` — `info` is the OQ-resolution from DEC-006); envelope rendering for 5 of 13 codes + generic catch-all `specforge.tool.internal_error` with correlationId; stderr-only logging with `SPECFORGE_LOG_LEVEL`/`SPECFORGE_LOG_FILE` env vars; dogfood `.specforge.json` at repo root as part of Done Criteria. ~25-30 new unit tests + ITEM-001 architecture test continues. |
| `REV-ITEM-003-001` | `ART-ITEM-003` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Core-only consolidation of DEC-004 identifier rules. Locks: 5 Core services (`IdParser` pure no-I/O parsing of 4 forms — atomic/composite REV/descriptive ART/qualified; `IdValidator` with typed exceptions; `TitleSlug` validate + FromTitle normalization; `KindRegistry` with core 5 + per-package extraKinds and reserved-kind check; `IIdAllocator` interface-first with non-tombstone-aware default — ITEM-010 will decorate); `ILedgerReader` thin layer for allocator's filesystem reads; 3 new typed Core exceptions (`SpecforgeInvalidIdentifierException`, `SpecforgeReservedKindException`, `SpecforgeKindExhaustedException`) + 3 ToolExceptionMapper rows; retroactive reserved-kind check wired into `ConfigValidator` from ITEM-002. Dogfood test in Done Criteria: every identifier in current ledger files must parse. ~40-50 new unit tests. |
| `REV-ITEM-004-001` | `ART-ITEM-004` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Core-only realization of DEC-005 embedded-resource mechanism. Locks: `Specforge.Core.csproj` `<EmbeddedResource Include="..\..\spec\skills\**\*.*" Exclude="...\.gitkeep" LogicalName="specforge.skills/%(RecursiveDir)%(Filename)%(Extension)" />`; first `YamlDotNet` NuGet introduction with pinned version in `Directory.Packages.props`; 5 Core types (`EmbeddedSkill`, `EmbeddedSkillFile`, `SkillFrontmatter`, `SkillFrontmatterParser`, `IEmbeddedSkillCatalog`/`EmbeddedSkillCatalog`, `SkillCatalogValidator`); one-shot startup validation (broken skill → fail-fast server start with stderr Error log + non-zero exit code; empty catalog OK); name-vs-directory consistency check; tolerated extra frontmatter keys; 1 new typed Core exception (`SpecforgeEmbeddedSkillNotFoundException`) + envelope mapping; `spec/skills/.gitkeep` anchor created. Test fixture pattern: test assembly embeds fixtures under `test.skills/` prefix to avoid prod collision. ~20-25 new unit tests. |
| `REV-ITEM-005-001` | `ART-ITEM-005` | User | Approved | 2026-05-28 | Approved on first review (no refinements). First end-to-end skill flow. Locks: `ISkillInstaller` (Core) + `ISkillInstallTargetResolver` (Core, default `Environment.SpecialFolder.UserProfile`, DI-swappable so tests never touch real `~/.claude` or `~/.agents`); `SkillInstallAgent` enum (ClaudeCode, Codex, All); `SkillInstallResult` + `SkillInstallAgentResult` records; `install_skills` MCP tool (4th tool, first to perform user-wide FS writes) with rich JSON Schema (`agent` enum + `dryRun`); per-agent partial-failure semantics — one agent fails → FAILURE envelope, `data.partialResult` carries both successful-agent's full state and failing-agent's pre-failure progress, no rollback; always-overwrite reinstall; byte-for-byte writes (no transformation); 1 new typed Core exception (`SpecforgeSkillInstallException`) with structured payload + envelope mapping (12th code); empty catalog behavior preserved (zero counts, no FS traversal); single-agent invocation absents the other agent from result (not zero — absent). ~30-40 new unit tests in 5 new test classes + extension to `ToolExceptionMapperTests`. 2 evidence transcripts (empty-catalog smoke + partial-failure). |
| `REV-ITEM-006-001` | `ART-ITEM-006` | User | Approved | 2026-05-28 | Approved on first review (after the round-1 fork: re-init = full `packages[]` replacement + schema upgrade). Biggest tool item in Stage 1 — `init` consolidates 5 DECs' worth of file-writing into one operation. Locks: 5th MCP tool `init` with rich JSON Schema; 12 new Core types (InitOptions, InitPackageInput, InitResult, InitSectionResult, BehavioralFilesScope, ConfigWriter, SpecforgeMdGenerator, TaggedBlockMerger, CodexOpenaiYamlWriter, EmbeddedTemplate, IEmbeddedTemplateCatalog/EmbeddedTemplateCatalog, ScaffoldEngine, IInitService/InitService); 2 new embedded-resource globs (`specforge.shared/**`, `specforge.templates/**`); always-overwrite for SPECFORGE.md + agents/openai.yaml; tagged-block-only edits for CLAUDE.md/AGENTS.md (user content outside block byte-preserved); scaffold mode non-destructive (existing files → SkippedPaths); atomic config write via temp-rename; tagged-block imbalance throws `specforge.tool.invalid_argument`. Dogfood idempotency in Done Criteria. ~50-60 new unit tests in 8 test classes + 3 evidence transcripts. No new error codes. |
| `REV-ITEM-007-001` | `ART-ITEM-007` | User | Approved | 2026-05-28 | Approved on first review (no refinements). First spec-graph item lands. Locks: 5 decision MCP tools (`list_decisions`, `get_decision`, `create_decision`, `set_decision_status`, `delete_decision`) — tool count 5 → 10; ledger row primitives reused by ITEM-008/009 (`LedgerRow`, `LedgerTable`, `LedgerTableParser`, `LedgerTableWriter` — first Markdig NuGet introduction); 3 ledger services (`IArtifactLedgerService`, `IHistoryLedgerService`, `IReviewLedgerService`) with row-by-LedgerId semantics; typed row records (`ArtifactRow`, `HistoryRow`, `ReviewRow`, `CommitRow`); `LifecycleStateMachine` encoding `spec/shared/document_lifecycle.md` transitions; `DecisionDocument` model + Markdig-backed parser dogfooded against DEC-001..008; `DecisionFileWriter` template instantiation; multi-effect atomic-ish operations (`create_decision` = file+ART row+history; `set_decision_status` Approved = file+ART row+history+REV row; `delete_decision` = file+ART row+REV cascade+history with tombstone evidence); pre-Approved gate + `confirm: true` requirement on delete; one-level cascade scope (REV rows only; indirect Supersedes refs surfaced by ITEM-010 `validate(aspect=links)`); 1 new typed Core exception `SpecforgeDeleteForbiddenException` with `currentStatus`/`allowedStates` payload + envelope mapping (13th code `specforge.lifecycle.delete_forbidden`); identifier acceptance bare-or-qualified per DEC-004. Round-trip ledger-table byte-equivalence in Done Criteria. ~80-100 new unit tests in 12 test classes + 2 evidence transcripts. |
| `REV-ITEM-008-001` | `ART-ITEM-008` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Item-side mirror of ITEM-007. Locks: 5 item MCP tools (`list_items`, `get_item`, `create_item`, `set_item_status`, `delete_item`) — tool count 10 → 15; `ItemDocument` record with `MissingRequiredSections` field; `ItemDocumentParser` (Markdig-backed, required-section-aware per `spec_item_contract.md`); `ItemFileWriter` instantiating `templates/item_spec.md`; `RequiredItemSections` static list as single source of truth in code; `RequiredItemSectionsContractTests` drift guard (reads `spec/shared/spec_item_contract.md` at test time and asserts in-code list matches); `ArtifactLedgerService` extended with kind-based routing (`ART-DEC-*` → `artifacts.md`, `ART-ITEM-*` → `items.md`); deliberate non-enforcement of required-section completeness in `set_item_status(Approved)` (deferred to ITEM-010 `validate(aspect=impact-coverage)`); `create_item` does not verify `dependsOn` target existence (deferred to ITEM-010 `validate(aspect=links)`); reuses every ITEM-007 infrastructure piece (ledger primitives, three ledger services, `LifecycleStateMachine`, `SpecforgeDeleteForbiddenException`, `ToolExceptionMapper` rows). 0 new NuGet packages, 0 new error codes, 0 new typed exceptions, 0 new embedded-resource globs. ~60-80 new unit tests in 8-9 test classes + 2 evidence transcripts. |
| `REV-ITEM-009-001` | `ART-ITEM-009` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Ledger-side spec-graph item. Locks: 5 ledger MCP tools (`append_history`, `append_review`, `append_commit`, `delete_review`, `delete_commit`) — tool count 15 → 20; `ICommitLedgerService` + `CommitLedgerService` (4th ledger service, first writer to `<package>/ledger/commits.md`); closed literal-target set `{(milestone), (multiple)}` for `append_history` via `HistoryLiteralTarget` constants; `IHistoryLedgerService.AppendAsync(string literalTarget, ...)` overload; `delete_review`/`delete_commit` have NO lifecycle gate (reviews/commits don't have lifecycle states per `document_lifecycle.md`) — gate = `confirm: true` only; tombstone-tolerant per-target REV seq and global CMT seq via existing "max(existing)+1" allocator rule (TombstoneAwareIdAllocator decorator for gap verification deferred to ITEM-010); first `append_commit` per package promotes `ART-LEDGER-COMMITS` from Placeholder to Draft (one-time, idempotent on subsequent calls); `append_commit` validates `gitRef` only against non-empty/no-whitespace/≤64-char rule (no SHA-shape regex; specforge is not a git client); `append_review` outcome is free-form (no enum gate at MVP); no `delete_history` tool (history is append-only audit log per DEC-007). 0 new NuGet packages, 0 new error codes, 0 new typed exceptions. ~50-60 new unit tests in 7 test classes + 2 evidence transcripts. |
| `REV-ITEM-010-001` | `ART-ITEM-010` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Final MCP tool — completes DEC-007's 21-tool catalog. Locks: `validate(aspect: ids\|links\|lifecycle\|impact-coverage\|all)` — read-only, no `dryRun`, no mutations; structured `data.findings: [{aspect, severity, target?, message, suggestion?}]` with severities `{error, warning, info}`; findings-as-data (SUCCESS envelope even when errors present); `aspect=all` never short-circuits; 4 aspect validators with explicit severity rules (`Supersedes` dangling = error vs `Depends on` dangling = warning per ITEM-008 invariant; Approved-without-REV = warning; Approved-with-missing-sections = error gate violation); `TombstoneAwareIdAllocator` decorator over ITEM-003's base allocator (deferred through 3 prior items, lands here); `IValidationService` orchestrator; `ValidationFinding`/`ValidationResult` records. **Public-contract refinement**: tombstone-detail format formalized as `^Tombstone (<deleted-id>):` — forward-compatible amendment to ITEM-007/008/009 detail wording; `TombstoneDetailFormat.Format(...)` helper centralizes formatting for Stage 2 implementations. `DogfoodValidationTests.ValidateAll_LiveSpecGraph_HasNoErrors` as marquee proof — validate against own spec graph must return `errorCount=0`. 0 new NuGet packages, 0 new error codes, 0 new typed exceptions, 0 new globs. ~80-100 new unit tests in 8 test classes + 2 evidence transcripts. |
| `REV-ITEM-011-001` | `ART-ITEM-011` | User | Approved | 2026-05-28 | Approved on first review (no refinements). Audit + centralization pass. Locks: `Specforge.Mcp/Errors/` canonical surface — `SpecforgeErrorCode` const store (14 codes), `MappedError` record, consolidated `ToolExceptionMapper`, `IEnvelopeRenderer`/`EnvelopeRenderer`, `EnvelopeJson` record; **audit discovers + closes real gap**: `specforge.id.not_found` used by ITEM-009 delete_review/delete_commit but missing from DEC-007 catalog — ITEM-011 amends DEC-007 additively (13 → 14 codes) and introduces `SpecforgeIdNotFoundException` (12th typed exception); `tool.internal_error` is the only catch-all path AND the only code carrying `correlationId` GUID + stderr log emission per DEC-003; 5 invariant tests prevent future drift (`EveryTypedExceptionHasMapperArm`, `CatalogDriftTests` via ITEM-007's `LedgerTableParser` parsing DEC-007 markdown, `OrphanCodeTests`, `NoStringLiteralCodesTests`, `SpecforgeErrorCodeTests`); all 14 codes reachable via fixture-trigger transcript. Refactors `DeleteReviewTool`/`DeleteCommitTool` to throw typed exception instead of inline emission. Single-source-of-truth pattern: every tool calls `EnvelopeRenderer.Render(ex)` or `EnvelopeRenderer.InvalidArgument(...)` — no direct envelope construction. ~40-60 new tests + 2 evidence transcripts. 0 new tools, 0 new NuGet packages, +1 typed exception, +1 error code (DEC-007 amendment). |
| `REV-ITEM-012-001` | `ART-ITEM-012` | User | Approved | 2026-05-28 | Approved after one round of self-review refinements. First content-authoring item. Locks: 6 SKILL.md bodies at `spec/skills/<kebab>/SKILL.md` (`draft-decision`, `review-decision`, `draft-item`, `impact-assessment`, `validate-spec-graph`, `adopt-existing-project`); per-skill content outlines (Frontmatter / First-paragraph narrative / When to use / Steps / Tools used / Shared docs referenced / Cross-skill references) detailed enough for fresh-session authoring; 6 new descriptive `ART-SKILL-*` rows; section name "Shared docs referenced" covers both `spec/shared/*.md` and `spec/templates/*.md` (clarified for `draft-decision`/`draft-item` which naturally reference templates alongside shared docs); `SkillContentReferenceTests` integration test with three theories — tool-name two-phase check (exact-match against 21-tool catalog + shape-regex fallback for `_`-containing typos; single-word typos like `infoo` caught at runtime not test time), shared-doc resolution, cross-skill name validation; ITEM-011 reflection surface optional (hard-coded 21-name fallback per DEC-007); reuses ITEM-004's embedded glob and ITEM-005's installer as-is. **Self-review caught 4 defects + 1 design improvement**: 2 description fields over 200-char limit (209/204 → 192/177), ITEM-011 forward-ref while not in Depends on (→ ITEM-010 with fallback paragraph), inconsistent regex spellings (unified to `^[a-z][a-z_]+[a-z]$`), section-shared-vs-templates mismatch between outline+regex+content+test (all unified), and the regex blind spot on single-word tools (`init`, `info`, `validate`) reworked into a two-phase check via exact-match + regex-fallback. 0 code, 0 tools, 0 NuGet packages, 0 error codes, 0 typed exceptions, 0 globs. ~20-30 effective test cases + 2 evidence transcripts. |
| `REV-ITEM-013-001` | `ART-ITEM-013` | User | Approved | 2026-05-28 | Approved after one round of self-review refinements (3 defects fixed: relative path wrong by 2 levels in Links, misleading `specforge.exe info` standalone verification, style inconsistency on architecture-test reference). **FINAL Stage 1 item — Stage 1 milestone reached: all 13 items Approved on 2026-05-28.** Locks: top-level `README.md` revision (tagline, what-is-specforge, quick start, requirements, doc links, status line) + new `docs/getting-started.md` walkthrough (pre-install requirements; build-from-source vs pre-built binary install paths per DEC-001; `.mcp.json` + `agents/openai.yaml` registration; bootstrap quartet `init` → `install_skills` → `use_package` → `validate`; first-decision + first-item authoring walkthroughs; upgrade ritual using DEC-006's opportunistic-write schema-upgrade chain). New artifact `ART-DOCS-GETTING-STARTED` (Not started until Stage 2). Revises `ART-README-TOP` (content change planned, status unchanged). `DocsContentReferenceTests` three-theory integration test against 21-tool catalog + 6-skill catalog + closed canonical-file-path set. 2 evidence transcripts (bootstrap walkthrough + upgrade walkthrough). 0 code, 0 tools, 0 NuGet packages, 0 error codes, 0 typed exceptions, 0 globs. ~10-20 effective test cases. **Stage 2 implementation unblocked.** |
