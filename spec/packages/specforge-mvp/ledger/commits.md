# Commit Ledger — specforge-mvp

Status: Draft

Append-only commit / push provenance. Promoted from Placeholder → Draft on 2026-05-29 when the first implementation commit (ITEM-001) landed.

| LedgerId | Commit SHA | Linked artifacts/items | Branch | Date | Note |
|---|---|---|---|---|---|
| `CMT-001` | `13a306d` | `ART-ITEM-001` | `master` | 2026-05-29 | ITEM-001 solution bootstrap. Initial repo commit: full Stage 0/1 spec + 3-project .NET 10 scaffold (Specforge.Core, Specforge.Mcp [AssemblyName=specforge], Specforge.Tests) + Directory.Build.props/Packages.props + global.json + xUnit architecture boundary test. Build 0 warnings; 2 tests pass; publish produces `bin/specforge.exe`. Deliberate-negative-test confirmed the boundary test catches drift (reverted). |
| `CMT-002` | `284e3be` | `ART-ITEM-002` | `master` | 2026-05-29 | ITEM-002 config model + MCP host + first 3 tools (`list_packages`, `use_package`, `info`). Core config pipeline (discovery/parse/validate/version-gate per DEC-002/006), 5 typed exceptions, `SessionState`, `BinaryInfo`. Mcp stdio host on `ModelContextProtocol.Core` 1.3.0 (low-level surface chosen over the `ModelContextProtocol` meta-package to avoid a `Microsoft.Extensions.Hosting` dependency, per DEC-003), `IMcpTool` pattern, `ToolExceptionMapper` (5 codes + `internal_error`), stderr-only logging. Dogfood `.specforge.json`; smoke evidence `itm002-smoke.txt`. Build 0 warnings; 35 tests pass. |
| `CMT-003` | `0605d29` | `ART-ITEM-003` | `master` | 2026-05-29 | ITEM-003 identifier infrastructure (Core-only): `IdParser`/`IdRegexes` (4 DEC-004 forms), `IdValidator`, `TitleSlug`, `KindRegistry` (reserved-kind check), `IIdAllocator`/`IdAllocator` + `ILedgerReader`/`LedgerReader`. 3 new typed exceptions (`specforge.id.*`) + mapper arms. Retrofit: `ConfigValidator.EnforceReservedKinds` rejects reserved `extraKinds` at load time. Build 0 warnings; 114 tests pass (79 new). Evidence `itm003-parser.txt`. |
| `CMT-004` | `a86526f` | `ART-ITEM-004` | `master` | 2026-05-29 | ITEM-004 embedded skill catalog (Core-only): `SkillFrontmatterParser` (YamlDotNet), `IEmbeddedSkillCatalog`/`EmbeddedSkillCatalog`, `SkillCatalogValidator`, `SpecforgeEmbeddedSkillNotFoundException` (`specforge.skills.catalog_missing`, 11th code). `Core.csproj` embeds `spec/skills/**`; `spec/skills/.gitkeep`. Mcp runs startup validation (broken skill → exit 1). Fixed MSBuild `%(RecursiveDir)` backslash-separator bug in the catalog. Build 0 warnings; 131 tests pass (16 new). Evidence `itm004-startup-fail.txt`. |
| `CMT-005` | `302ba40` | `ART-ITEM-005` | `master` | 2026-05-29 | ITEM-005 skill installer + `install_skills` (4th tool). `ISkillInstaller`/`SkillInstaller` (byte-for-byte, always-overwrite, dryRun, overwrite detection, no-rollback partial failure), `ISkillInstallTargetResolver` (UserProfile; DI-swappable). `SpecforgeSkillInstallException` (`specforge.skills.install_failed`, 12th code) with `partialResult`. Extended `EmbeddedSkill` with `SkillMarkdown`. Build 0 warnings; 146 tests pass (16 new). Evidence `itm005-smoke.txt` + `itm005-partial-failure.txt`. |
| `CMT-006` | `ef32f58` | `ART-ITEM-006` | `master` | 2026-05-29 | ITEM-006 init tool (5th tool) + embedded templates + project-level files. `InitService` orchestrates `ConfigWriter` (atomic, canonical), `SpecforgeMdGenerator`, `TaggedBlockMerger` (imbalance → `invalid_argument`), `CodexOpenaiYamlWriter`, `ScaffoldEngine` (non-destructive), `EmbeddedTemplateCatalog` (`specforge.shared/**` + `specforge.templates/**` globs). `SpecforgeInvalidArgumentException` carrier + mapper arm. Build 0 warnings; 178 tests pass (32 new). Evidence: scaffold-fresh / dogfood-idempotency (byte-equal re-run) / tagged-block-imbalance. |
| `CMT-007` | `ad2f2e9` | `ART-ITEM-007` | `master` | 2026-05-29 | ITEM-007 decision tools (tool count 5 → 10) + ledger primitives + lifecycle. `Ledger/*` (row primitives, line-surgical `LedgerDocument`, 3 ledger services), `LifecycleStateMachine` (8 states), `DecisionDocument`/Parser (Markdig)/Writer, `DecisionService` (multi-effect create/set/delete + REV cascade). `SpecforgeDeleteForbiddenException` (`specforge.lifecycle.delete_forbidden`, 13th code). 5 decision tools. Markdig added to Core. Build 0 warnings; 217 tests pass (39 new). Evidence itm007-dogfood-parse.txt + itm007-create-delete.txt. |
| `CMT-008` | `986efba` | `ART-ITEM-008` | `master` | 2026-05-30 | ITEM-008 item tools (tool count 10 → 15). `ItemDocument`/Parser (Markdig, required-section-aware)/Writer, `RequiredItemSections`, `ItemService` (mirror of DecisionService). Ledger routing: `ART-ITEM-*` → items.md, `ART-DEC-*`/others → artifacts.md; `IdAllocator` ITEM kind scans items.md. 5 item tools; no new codes/NuGet. Build 0 warnings; 238 tests pass (21 new). Evidence itm008-dogfood-parse.txt + itm008-create-delete.txt (full serial binary round-trip). |
| `CMT-011` | `e427b77` | `ART-ITEM-011` | `master` | 2026-05-30 | ITEM-011 error-envelope centralization + audit (no new tools; still 21). `Core/Diagnostics/SpecforgeErrorCode` (14 codes) + `SpecforgeIdNotFoundException`; `Mcp/Errors/{MappedError, ToolExceptionMapper (consolidated), IEnvelopeRenderer, EnvelopeRenderer}`. All 13 typed exceptions + every Mcp site reference the const store (no code-literal scatter). 4 ledger tools throw `SpecforgeIdNotFoundException` (was inline). DEC-007 amended 13→14 (`specforge.id.not_found`). 5 drift-guard tests (mapper-arm/orphan/catalog/no-literal/code-shape). Build 0 warnings; 326 tests pass. Evidence itm011-catalog-render.txt + itm011-correlation-id.txt. |
| `CMT-010` | `9adcc26` | `ART-ITEM-010` | `master` | 2026-05-30 | ITEM-010 validate tool (tool count 20 → 21; DEC-007 catalog complete). `Validation/*`: `validate(aspect=ids\|links\|lifecycle\|impact-coverage\|all)` read-only findings-as-data; 4 aspect validators + `ValidationService` (no short-circuit); `TombstoneAwareIdAllocator` decorator (`VerifyGapsAsync`); `TombstoneDetailFormat` locks `Tombstone <id>:` contract — all 4 delete tools retrofitted. Dogfood fix: removed 13 stale `ART-ITEM-*` rows from artifacts.md (belong only in items.md per ITEM-008 routing). `DogfoodValidationTests` green (0 errors). No new NuGet/codes/exceptions/globs. Build 0 warnings; 318 tests pass (39 new). Evidence itm010-dogfood-validate-all.txt + itm010-sad-path.txt. |
| `CMT-009` | `05d8d37` | `ART-ITEM-009` | `master` | 2026-05-30 | ITEM-009 ledger append + delete tools (tool count 15 → 20). `append_history` (LedgerId or closed literal `(milestone)`/`(multiple)` via `HistoryLiteralTarget` + `IHistoryLedgerService.AppendLiteralAsync`), `append_review` (REV per-target seq + Reviewed event), `append_commit` (CMT global seq + Implemented event + first-call ART-LEDGER-COMMITS Placeholder→Draft promotion), `delete_review`/`delete_commit` (confirm gate, no lifecycle gate, tombstone). New `ICommitLedgerService`/`CommitLedgerService` (4th ledger service, first commits.md writer); `CommitRow` Linked/Branch now code-spans. `specforge.id.not_found` emitted inline (deferred to ITEM-011 for catalog/exception); SpecforgeToolBase += IdNotFound/ConfirmRequired/ToArtifactId/BareTarget. No new NuGet/catalog-codes/globs. Build 0 warnings; 279 tests pass (41 new). Evidence itm009-append-cycle.txt + itm009-delete-cycle.txt. |
