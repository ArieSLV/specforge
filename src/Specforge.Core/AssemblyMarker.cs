namespace Specforge.Core;

/// <summary>
/// Public marker type used by the architecture test (ITEM-001) to locate the
/// Specforge.Core assembly via <c>typeof(AssemblyMarker).Assembly</c>.
/// Body is intentionally empty; real domain types arrive in ITEM-002 onward.
/// </summary>
public static class AssemblyMarker
{
}
