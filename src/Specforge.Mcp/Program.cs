// ITEM-001 bootstrap entry point.
// Real behavior — config discovery, MCP stdio loop, tool registration —
// arrives in ITEM-002 (config + first tools) onward.
// At this stage the binary launches and exits with code 0.

return 0;
